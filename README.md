# custom-reviews-plugin-wp
A lightweight plugin for the feedback system for WordPress. It is written in Vanilla JS using the WP AJAX API. No dependencies (no jQuery), optimized for modern themes (e.g. Twenty Twenty-Five)
